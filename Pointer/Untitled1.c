#include <stdio.h>
#include <math.h>

int main()
{
    int a, b, c;
    ///a = -1;
    ///b = -1;
    ///c = -1 + -1;
    ///c = 0 * 1000 -1 + 9;
    scanf("%d", &c);
    c = sqrt(c);
    printf("%d", c);
    return 0;
}
